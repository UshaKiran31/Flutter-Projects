// ignore_for_file: file_names

class Question {
  //  instance fields are non-nullable by default.
  //if you want to explicitly declare a nullable instance field, you can use the ? operator after the type.

  String? questionText;
  bool? questionAnswer;
  Question(this.questionText, this.questionAnswer);
}
